import { HighlightLabelDirective } from './highlight-label.directive';

describe('HighlightLabelDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightLabelDirective();
    expect(directive).toBeTruthy();
  });
});
